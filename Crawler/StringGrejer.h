#pragma once
#include <MinaStringOperations.h>
/*
void ReplaceAll(std::string* Stringen,std::string StringToReplace, std::string StringToReplaceWith)
{
    size_t index = 0;
    while (true) {
        index = Stringen->find(StringToReplace, index);
        if (index == std::string::npos)
        {
            break;
        }
        Stringen->replace(index, StringToReplace.size(), StringToReplaceWith);

        index += StringToReplaceWith.size();
    }
}
std::string ReplaceAll(std::string Stringen,std::string StringToReplace, std::string StringToReplaceWith)
{
    size_t index = 0;
    while (true) {
        index = Stringen.find(StringToReplace, index);
        if (index == std::string::npos)
        {
            break;
        }
        Stringen.replace(index, StringToReplace.size(), StringToReplaceWith);

        index += StringToReplaceWith.size();
    }
    return(Stringen);
}
*/